import { ref, inject, provide, defineComponent, onMounted } from 'vue'
import axios from 'axios'
import VueAxios from 'vue-axios'

export default {
    ref,
    inject,
    provide,
    defineComponent,
    onMounted,
    axios,
}